{
  "name": "anorthoscope-simulator",
  "version": "2.0.0",
  "description": "Anorthoscope (anorthoscope) simulator — TypeScript + Canvas rebuild",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
